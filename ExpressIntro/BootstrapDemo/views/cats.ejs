<%- include('partials/head') %>

    <%- include('./partials/navbar') %>

        <h1>All The Cats</h1>
        <% if(cats) { %>
            <Ul>
                <% for (const cat of cats) { %>
                    <li>
                        <%= cat %>
                    </li>
                    <% } %>
            </Ul>
            <% } %>
                <%- include('partials/footer') %>